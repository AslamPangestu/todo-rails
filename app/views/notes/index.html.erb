<%= render 'flash' %>

<% content_for :title, "Notes" %>

<table class="table table-bordered">
  <tbody>
    <% @notes.each do |note| %>
      <%= render note %>
    <% end %>
  </tbody>

  </table>

<%= link_to "Delete Completed Items", home_trash_em_path, class:'btn btn-danger',data: {
    turbo_method: :get,
    turbo_confirm: "Are you sure?"
  } %>

  <%= link_to "Delete All Items", home_trash_em_all_path, class:'btn btn-danger',data: {
    turbo_method: :get,
    turbo_confirm: "Are you sure?"
  } %>
